/* ====================================================================
   CONFIGURATION DES COMPTES (optionnel)
   --------------------------------------------------------------------
   - Si tu LAISSES VIDE : l'appli marche en mode "profils" locaux
     (chaque personne choisit un profil, sauvegarde dans son navigateur).
   - Si tu REMPLIS les 2 valeurs ci-dessous avec ton projet Supabase :
     l'appli passe en mode "comptes" (email + mot de passe, synchronisé
     entre tous les appareils).

   Comment obtenir ces 2 valeurs (gratuit, 5 min) :
   voir le fichier GUIDE-SUPABASE.md fourni.
   ==================================================================== */

window.SUPABASE_URL = "";       // ex: "https://abcd1234.supabase.co"
window.SUPABASE_ANON_KEY = "";  // ex: "eyJhbGciOi...." (clé "anon public")
